<main>
    <form class="theater-form" method="POST">
        <h1>Edit Theater</h1>
        <div>
            <label for="title">Theater Title:</label>
            <input type="text" placeholder="Theater name" name="title" value="{{play.title}}">
        </div>
        <div>
            <label for="description">Theater Description:</label>
            <textarea placeholder="Description" name="description">{{play.description}}</textarea>
        </div>
        <div>
            <label for="imageUrl">Image url:</label>
            <input type="text" placeholder="Image Url" name="image" value="{{play.image}}">
        </div>
        <div class="check">
            <label for="check-box">Public</label>
            <input type="checkbox" name="isPublic" {{#if isChecked}}checked{{/if}} id="check-box">
        </div>
        <button class="btn" type="submit">Submit</button>
    </form>
</main>